import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'za.co.mhi.service.sphere',
  appName: 'Service Sphere',
  webDir: 'dist/service-sphere-app',
  bundledWebRuntime: false
};

export default config;
